package com.noteability.mynote.ui.theme

import androidx.compose.ui.graphics.Color

// Blue Series (Primary Brand)
val BluePrimary = Color(0xFF13A4EC) // Original brand color
val Blue10 = Color(0xFF003258)      // Dark blue for text on light containers
val Blue30 = Color(0xFF004D80)      // Darker blue
val Blue40 = Color(0xFF13A4EC)      // Standard primary for Light Theme
val Blue80 = Color(0xFF8CD4F8)      // Lighter variant for Dark Theme primary
val Blue90 = Color(0xFFE0F2FE)      // Very light blue (Container/Secondary)

// Neutral Series (Backgrounds, Surfaces)
val Neutral10 = Color(0xFF101C22)   // Dark background
val Neutral20 = Color(0xFF1E2F38)   // Slightly lighter dark background
val Neutral40 = Color(0xFF555555)   // Grey 700
val Neutral60 = Color(0xFF999999)   // Grey 500
val Neutral90 = Color(0xFFE2E8F0)   // Light Gray
val Neutral95 = Color(0xFFF6F7F8)   // Light background
val Neutral99 = Color(0xFFFCFCFC)   // Nearly white

// Text Colors
val TextBlack = Color(0xFF101C22)
val TextGray = Color(0xFF64748B)
val TextLightGray = Color(0xFF94A3B8)

// Functional Colors
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)
val ErrorRed = Color(0xFFB3261E)